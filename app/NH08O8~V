server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA React: siempre devolver index.html si no existe el fichero
    location / {
        try_files $uri /index.html;
    }
}
